# Reproducibility - Group 3, Thesis Seminar II (TEP root-cause pipeline).
# A result that only runs on one machine is not a result: this pins the exact
# environment used to produce every number and figure in results/.
#
# Tested on Python 3.14.2 (Windows). requirements.txt cannot pin Python itself,
# so use that interpreter (or a venv created from it), then:
#     python -m pip install -r requirements.txt
#
# --- Core pipeline (scripts 01-09 + checkpoint): required to reproduce all results ---
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.1
pyreadr==0.5.6            # reads the Rieth TEP .RData files
pyarrow==25.0.1          # parquet feature caches (dev_features, dev_domain_features)
matplotlib==3.11.2       # publication figure (09_plot_label_efficiency.py)
#
# --- Week 3 (deep learning) - NOT imported by scripts 01-09 ---
# CPU build (~200 MB, no GPU needed for this data size). Install separately, since
# the +cpu build is served from PyTorch's own index, not PyPI:
#     python -m pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
# installed version at time of writing: torch==2.14.0+cpu
