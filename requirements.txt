# Reproducibility - TEP root-cause pipeline (Thesis Seminar II).
# A result that only runs on one machine is not a result: this pins the exact
# environment used to produce every number and figure in results/.
#
# Tested on Python 3.14.2 (Windows). requirements.txt cannot pin Python itself,
# so use that interpreter (or a venv created from it), then:
#     python -m pip install -r requirements.txt
#
# --- Core: alone it runs every step that does not import torch ---
numpy==2.5.3
pandas==3.0.5
scikit-learn==1.9.1
pyreadr==0.5.6            # reads the Rieth TEP .RData files
pyarrow==25.0.1          # parquet feature caches (dev_features, dev_domain_features)
matplotlib==3.11.2       # figures (09 and 13; 11 also plots its training curves)
#
# --- Networks and agent: torch, imported by scripts 11, 12, 14, 16, 17, 18 and 21 ---
# 11_train_dl.py trains the MLP and the 1D-CNN and saves one checkpoint per fold;
# 12_agente_v1.py reuses those checkpoints and retrains nothing. 14, 18 and 21 load
# the same checkpoints; 16 and 17 train the networks again, per budget and for timing.
# CPU build (~200 MB, no GPU needed for this data size). Install separately, since
# the +cpu build is served from PyTorch's own index, not PyPI:
#     python -m pip install torch==2.14.0 --index-url https://download.pytorch.org/whl/cpu
# installed version at time of writing: torch==2.14.0+cpu
#
# The agent needs nothing beyond the above: its reasoning step is rule-based, so
# there is no LLM client, no API key and no network access anywhere in the pipeline.
